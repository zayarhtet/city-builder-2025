image: ibmjava:jre

stages:
  - build
  - test

build:
  stage: build
  script: ls $JAVA_HOME/bin
  artifacts:
    paths:
     - TestBasic.*


test:
  stage: test
  script: /opt/ibm/java/jre/bin/java -cp ./lib/junit-4.13.2.jar:./lib/hamcrest-core-1.3.jar:./classes:. org.junit.runner.JUnitCore TestBasic
